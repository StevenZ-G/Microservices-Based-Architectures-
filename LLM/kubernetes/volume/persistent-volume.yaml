apiVersion: v1
kind: PersistentVolume
metadata:
  name: llm-persistent-volume
spec:
  capacity:
    storage: 5Gi  # Tamaño del volumen
  accessModes:
    - ReadWriteMany  # Permite que varios Pods lean y escriban
  hostPath:
    path: /llm/data  # Ruta en el nodo (para desarrollo)
